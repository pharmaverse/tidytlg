# Adds a continuous bottom border under a row

Adds a continuous bottom border under a row

## Usage

``` r
row_border(row)
```

## Arguments

- row:

  `numeric` the row of the table

## See also

Other border_functions: [`col_borders()`](col_borders.md),
[`no_borders()`](no_borders.md), [`single_border()`](single_border.md),
[`spanning_borders()`](spanning_borders.md)
