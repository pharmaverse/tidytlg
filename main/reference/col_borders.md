# Adds borders under cells in a column

Adds borders under cells in a column

## Usage

``` r
col_borders(col, rows)
```

## Arguments

- col:

  `numeric` the column of the table

- rows:

  `numeric` the range of rows to include

## See also

Other border_functions: [`no_borders()`](no_borders.md),
[`row_border()`](row_border.md), [`single_border()`](single_border.md),
[`spanning_borders()`](spanning_borders.md)
